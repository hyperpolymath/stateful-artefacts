# Toolchain pins for stateful-artefacts (asdf / mise format)
# Real toolchain: Zig builds the C-ABI/FFI seam; just is the task runner;
# Idris2 typechecks the ABI only.
zig 0.15.2
just 1.40.0
# idris2 0.7.0            # optional — ABI typecheck only
# rust nightly           # re-add when the reposystem re-transfer lands (no Rust yet)
