// SPDX-License-Identifier: CC-BY-SA-4.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= TEST-NEEDS: stateful-artefacts

== CRG Grade: see docs/status/READINESS.adoc

The CRG grade is an asserted human/AI-agent assessment, not derived from this
document — see link:READINESS.adoc[READINESS.adoc] for the current grade and
evidence gates.

== Current State (updated on instantiation cleanup)

[cols="2,1,4", options="header"]
|===
| Category | Count | Details

| *Source modules* | 6 | 3 Idris2 ABI (Foreign, Layout, Types), 2 Zig FFI (build.zig, main.zig), 1 Zig core (record.zig)
| *Unit tests* | 7 | Inline `test` blocks in `main.zig` (3) and `record.zig` (4, via `zig build test`'s core-module step)
| *Integration tests* | 9 | `src/interface/ffi/test/integration_test.zig` — real tests exercising the library entirely through its C ABI (lifecycle, null-safety, strings, version, and the artefact-state-record's phase/verification state machines)
| *Aspect tests* | 1 | `tests/aspect_tests.sh` (SPDX headers, no-dangerous-proof-patterns, Idris2<->Zig symbol parity)
| *Workflow tests* | 1 | `tests/workflows/validate_workflows_test.sh`
| *Benchmarks* | 3 | `benches/template_bench.sh` (Zig build, Zig tests, workflow validation)
| *Fuzz tests* | 0 | `tests/fuzz/README.adoc` scaffold with harness instructions — none configured yet
|===

NOTE: `scripts/validate-template.sh` and
`tests/e2e/template_instantiation_test.sh` — a generic RSR-conformance
checker and an instantiate-the-template-into-a-temp-dir E2E test — were
template-only machinery useful only pre-instantiation. Both have been
removed; `just validate` covers required-file/metadata compliance for this
already-instantiated repo.

== Test Results Summary (`zig build test`, Zig 0.15.2)

----
Build Summary: 9/9 steps succeeded; 16/16 tests passed
----

== CI Coverage

* `.github/workflows/e2e.yml` — builds the FFI seam and runs
  `zig build test --summary all`, then `tests/aspect_tests.sh`, on every push/PR
  touching `src/**` or `tests/**`.
* `.github/workflows/quality.yml`, `codeql.yml`, `scorecard.yml`,
  `secret-scanner.yml`, `hypatia-scan.yml` — quality/security gates.

== Known Gaps

* [ ] No fuzz harness configured (`tests/fuzz/README.adoc` documents how to
  add one when the library grows parsers or untrusted-input surfaces).
* [ ] `src/bridges/verisimdb-feed.sh` is a PROVISIONAL local stub, not
  covered by the test suite — it is not yet part of the re-transferred
  domain content.
* [ ] The reposystem domain content itself (the actual estate-coordination
  logic) has not yet been re-transferred — see
  link:../RE-TRANSFER-RUNBOOK.adoc[RE-TRANSFER-RUNBOOK.adoc]. Until it
  lands, this document only covers the v0 artefact-state-record slice.

== Next Steps

* [ ] Integrate `tests/workflows/validate_workflows_test.sh` and
  `benches/template_bench.sh` into CI (currently local-only).
* [ ] Generate test coverage reports.
* [ ] Add fuzz coverage once parser/untrusted-input surfaces exist.
